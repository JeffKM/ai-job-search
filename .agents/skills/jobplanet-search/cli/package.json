{
  "name": "jobplanet-cli",
  "version": "1.0.0",
  "description": "CLI for searching jobs on JobPlanet (잡플래닛) via Scrapling fetch + bun wrapper",
  "type": "module",
  "main": "src/cli.ts",
  "bin": { "jobplanet-search": "src/cli.ts" },
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test --timeout 120000",
    "typecheck": "tsc --noEmit",
    "setup": "cd ../fetch && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt && .venv/bin/scrapling install"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/bun": "latest"
  }
}
